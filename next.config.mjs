/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    images: {
        domains: ['https://dental-backend-mf9e.onrender.com','https://res.cloudinary.com']
    }
};

export default nextConfig;
